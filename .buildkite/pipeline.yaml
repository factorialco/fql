docker-compose#v3.7.0: &compose_config
  run: app
  config: .buildkite/docker-compose.yaml
steps:
  - label: Sorbet
    command: srb tc
    agents:
      queue: test
    plugins:
      docker-compose#v3.7.0:
        <<: *compose_config
  - wait
  - label: Lint
    command: rubocop
    agents:
      queue: test
    plugins:
      docker-compose#v3.7.0:
        <<: *compose_config
  - label: Test
    command: rspec
    agents:
      queue: test
    plugins:
      docker-compose#v3.7.0:
        <<: *compose_config
